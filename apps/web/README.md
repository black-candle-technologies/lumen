# Lumen Web

SvelteKit control surface for Lumen.

This app is built as static assets for the Tauri desktop shell and can also run
as the narrow web control surface described in the project architecture.

Common commands from the repository root:

```sh
pnpm dev:web
pnpm check:web
pnpm build:web
```
