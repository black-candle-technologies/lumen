# Lumen Desktop

Tauri desktop shell for Lumen.

The desktop app embeds the static SvelteKit build from `../web/build`.
During development, Tauri starts the shared SvelteKit dev server from `apps/web`.

Common commands from the repository root:

```sh
pnpm dev:desktop
pnpm build:desktop
```
