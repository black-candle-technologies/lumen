# Desktop Frontend Notes

The desktop shell embeds the built SvelteKit app from `apps/web/build`.

Keep desktop-only frontend assets or notes here if the Tauri shell needs them later. Do not add a second application frontend here unless the shared web surface stops fitting the desktop product.
